# =============================================================
# Shared config — ไม่มี secret, commit ได้
# =============================================================

# Clerk redirect URLs (ใช้ได้ทั้ง dev และ production)
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/payment
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/payment
